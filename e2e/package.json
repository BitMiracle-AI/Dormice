{
  "name": "@dormice/e2e",
  "version": "0.0.0",
  "description": "Black-box end-to-end suite: boots the built daemon and drives it over the wire via the SDK",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dormice/sdk": "workspace:*",
    "e2b": "^2.31.0"
  }
}
